{
  "name": "css-example",
  "version": "1.0.0",
  "description": "only css example",
  "scripts": {
    "dev": "parcel serve assets/index.html --open -p 8080",
    "build": "parcel build assets/index.html --no-autoinstall --no-source-maps -d ./dist"
  },
  "author": "Joey Fang",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.10.3"
  },
  "dependencies": {}
}
